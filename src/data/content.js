// Content loader — all site copy lives in src/data/content.json
// (editable from the admin panel). Import { C } anywhere you need text.
import contentJson from './content.json';

export const C = contentJson;
